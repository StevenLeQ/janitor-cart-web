# .gitlab-ci.yml
image: node:18

before_script:
  - npm install

unit_tests:
  stage: test
  script:
    - echo "Running unit tests..."
    - npm test
  # artifacts:
  #   when: always
  #   paths:
  #     - rspec.xml
  #   reports:
  #     junit: rspec.xml
